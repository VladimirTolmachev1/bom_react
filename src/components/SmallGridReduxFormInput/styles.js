export default theme => ({
  marginDense: {
    marginTop: 8,
    marginBottom: 4,
  },

  cssLabel: {
    transform: 'translate(14px, 8px) scale(1)',
  },

  input: {
    padding: theme.spacing.unit * 0.8,
  },
});
